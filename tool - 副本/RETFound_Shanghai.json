{
    "tool_name": "RETFound_shanghai",
    "tool_description": "RETFound_shanghai is an open-source deep learning tool that provides pretrained weights on various retinal imaging datasets for automatic classification of Diabetic Retinopathy (DR) and Glaucoma.",
    "tool_author": "Yukun Zhou and Zheyuan Wang",
    "tool_version": "latest",
    "tool_url": "https://hub.docker.com/r/medlab123/retfound_shanghai",
    "demo_commands": [
      "docker run --rm -v /path/to/input:/src/data/input -v /path/to/output:/src/data/output RETFound_shanghai:latest --task DR --train_dataset aptos",
      "docker run --rm -v /path/to/input:/src/data/input -v /path/to/output:/src/data/output RETFound_shanghai:latest --task DR --train_dataset messidor",
      "docker run --rm -v /path/to/input:/src/data/input -v /path/to/output:/src/data/output RETFound_shanghai:latest --task DR --train_dataset idrid",
      "docker run --rm -v /path/to/input:/src/data/input -v /path/to/output:/src/data/output RETFound_shanghai:latest --task Glaucoma --train_dataset glaucoma_fundus",
      "docker run --rm -v /path/to/input:/src/data/input -v /path/to/output:/src/data/output RETFound_shanghai:latest --task Glaucoma --train_dataset papila"
    ],
    "input_schema": {
      "type": "object",
      "required": ["input", "output", "task", "train_dataset"],
      "properties": {
        "input": {
          "type": "string",
          "description": "Path to the input folder containing fundus images (e.g., PNG or JPG format)."
        },
        "output": {
          "type": "string",
          "description": "Path to the output folder where the prediction results will be saved."
        },
        "task": {
          "type": "string",
          "enum": ["DR", "Glaucoma"],
          "description": "Specify the classification task: DR (Diabetic Retinopathy) or Glaucoma."
        },
        "train_dataset": {
          "type": "string",
          "description": "Name of the dataset used to fine-tune the model (e.g., aptos, messidor, idrid, glaucoma_fundus, papila)."
        }
      }
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "results_csv": {
          "type": "string",
          "description": "Path to the output CSV file that contains prediction results for each image."
        }
      },
      "csv_format": {
        "columns": [
          "Name",
          "raw_Prediction",
          "Prediction",
          "confidence",
          "class_0_prob",
          "class_1_prob",
          "class_2_prob",
          "class_3_prob",
          "class_4_prob"
        ],
        "description": {
          "Name": "Image file name (same as in input folder)",
          "raw_Prediction": "Raw model output (original class index predicted by the model before any post-processing or mapping)",
          "Prediction": "Final prediction after task-specific mapping (e.g., AMD or Glaucoma may map raw classes to binary)",
          "confidence": "Predicted probability of the final class",
          "class_0_prob": "Probability of class 0",
          "class_1_prob": "Probability of class 1",
          "class_2_prob": "Probability of class 2",
          "class_3_prob": "Probability of class 3 (only for DR)",
          "class_4_prob": "Probability of class 4 (only for DR)"
        }
      }
    },
    "class_maps": {
      "DR": {
          "0": "No DR",
          "1": "Mild",
          "2": "Moderate",
          "3": "Severe",
          "4": "Proliferative DR"
        },
      "Glaucoma": {
          "0": "Normal",
          "1": "Suspected Glaucoma/Glaucoma"
        }
  }
}